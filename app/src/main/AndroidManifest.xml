<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FKJ_Consultants"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />

        <!-- Dashboards -->
        <activity android:name=".activities.UserDashboardActivity" />
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity android:name=".activities.BrandingActivity" />
        <activity android:name=".activities.SelectMaterialActivity" />
        <activity android:name=".activities.InventoryActivity" />
        <activity android:name=".activities.BasketActivity" />
        <activity android:name=".activities.ProfileAccountActivity" />
        <!-- Modules -->
        <activity android:name=".activities.CatalogActivity"
            tools:ignore="Instantiatable,MissingClass" />
        <activity android:name=".activities.QuotationGeneratorActivity" />
        <activity android:name=".activities.QuotationPreviewActivity"/>
        <activity android:name=".activities.QuotationHistoryActivity"/>
        <activity android:name=".activities.QuotationDetailsActivity"/>
        <activity android:name=".activities.InventoryListActivity"/>
        <!-- Chat -->
        <activity android:name=".activities.ChatListActivity" />
        <activity android:name=".activities.ChatActivity" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.AppFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
