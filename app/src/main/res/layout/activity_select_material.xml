<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/userDashboardLayout"
    android:layout_width="match_parent"
    android:layout_height="690dp"
    android:layout_marginTop="40dp"
    android:background="#f4f4f4"
    tools:context=".activities.SelectMaterialActivity">

    <!-- Title centered at top -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Product Catalog"
        android:textColor="#1B2C4A"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Buttons row below title, centered horizontally -->
    <LinearLayout
        android:id="@+id/buttonRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/btnViewBasket"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="View Basket"
            android:elevation="6dp"
            android:padding="12dp"
            android:src="@drawable/ic_shopping_cart"
            android:tint="#113866"
            tools:ignore="UseAppTint" />

        <ImageButton
            android:id="@+id/btnLogout"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="24dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Logout"
            android:elevation="6dp"
            android:padding="12dp"
            android:src="@drawable/ic_logout"
            android:tint="#113866"
            tools:ignore="UseAppTint" />
    </LinearLayout>

    <!-- Main content ScrollView below the buttons -->
    <ScrollView
        android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_above="@id/bottomNavigation"
        android:layout_below="@id/buttonRow"
        android:layout_alignParentBottom="true"
        android:clipToPadding="false"
        android:padding="16dp"
        android:paddingTop="24dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Search, Category Filter, and Sort Filter in one row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp"
                android:orientation="horizontal">

                <androidx.appcompat.widget.SearchView
                    android:id="@+id/searchViewProduct"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="2"
                    android:queryHint="Search by name..." />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <Spinner
                    android:id="@+id/spinnerCategoryFilter"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minHeight="48dp"
                    android:spinnerMode="dropdown" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <Spinner
                    android:id="@+id/spinnerSortFilter"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minHeight="48dp"
                    android:spinnerMode="dropdown" />
            </LinearLayout>

            <!-- Product List -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerViewMaterials"
                android:layout_width="match_parent"
                android:layout_height="358dp"
                android:nestedScrollingEnabled="false"
                android:padding="4dp" />

            <!-- Continue Button -->
            <Button
                android:id="@+id/btnContinue"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:backgroundTint="#113866"
                android:text="Proceed"
                android:textColor="#ffffff" />
        </LinearLayout>
    </ScrollView>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#123c6a"
        app:itemIconTint="@color/white"
        app:itemTextColor="@color/white"
        app:menu="@menu/bottom_nav_menu" />

</RelativeLayout>
